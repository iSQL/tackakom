@if(Request.IsAuthenticated) {
    
    <text>Cao <strong>@User.Identity.Name</strong>!
        [@Html.ActionLink("Log Off", "LogOff", "Account", null, new { id = "logOffId" }))]
        [@Html.ActionLink("Editing", "editing", "Event", null, new { id = "editId" })]</text>
    
}
else
{
    @:[ @Html.ActionLink("Log On", "LogOn", "Account", null, new { id = "logOnId" }) ]
}